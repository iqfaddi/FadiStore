name: Keep Supabase Alive

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -H "apikey: sb_publishable_TyktvjuEO4h4Qqs790RQMA_5hzlqEky" \
               -H "Authorization: Bearer sb_publishable_TyktvjuEO4h4Qqs790RQMA_5hzlqEky" \
               "https://hcuorfjumfurcxrvvsmw.supabase.co/rest/v1/products?select=id"
